<!DOCTYPE html>
<html>
<head>
  <title>User Table</title>
  <link rel="stylesheet" href="UserTable.css" />
</head>
<body>
  <h1>User Table</h1>
  
  <!-- Add User Form -->
  <h2>Add User</h2>
  <form action="/users/add" method="POST">
    
    <button type="submit">Add User</button>
  </form>
  
  <table border="1">
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Mobile</th>
      <th>Email</th>
      <th>Street</th>
      <th>City</th>
      <th>State</th>
      <th>Country</th>
      <th>Username</th>
      <th>Actions</th>
    </tr>
    <% users.forEach(user => { %>
      <tr>
        <td><%= user.firstName %></td>
        <td><%= user.lastName %></td>
        <td><%= user.mobile %></td>
        <td><%= user.email %></td>
        <td><%= user.street %></td>
        <td><%= user.city %></td>
        <td><%= user.state %></td>
        <td><%= user.country %></td>
        <td><%= user.username %></td>
        <td>
          <!-- Edit User Form -->
          <form action="/users/edit/<%= user._id %>" method="POST" style="display:inline-block;">
            
            <button type="submit">Edit</button>
          </form>

          <!-- Delete User Form -->
          <form action="/users/delete/<%= user._id %>" method="POST" style="display:inline-block;">
            <button type="submit">Delete</button>
          </form>
        </td>
      </tr>
    <% }) %>
  </table>
  
  <a href="/">Go Back to Login</a>
</body>
</html>
